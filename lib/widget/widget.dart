import 'package:flutter/material.dart';

class BuiledTicket extends StatelessWidget {
  final Widget prnWidget;

  const BuiledTicket({
    Key key,
    @required this.prnWidget,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(),
    );
  }
}
